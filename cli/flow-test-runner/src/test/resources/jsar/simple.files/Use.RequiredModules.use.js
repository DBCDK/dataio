/** @file Required modules */

/**  
 * This is executed just after the module system (Use.use.js) has been initialized,
 * hence modules which always need to be loaded may be added here.
 */
use( "ES5" );

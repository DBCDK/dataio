<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <parent>
    <artifactId>dataio-harvester-rr2datawell</artifactId>
    <groupId>dk.dbc</groupId>
    <version>1.0-SNAPSHOT</version>
  </parent>

  <artifactId>dataio-harvester-rr2datawell-ear</artifactId>
  <name>dataio-harvester-rr2datawell-ear</name>
  <packaging>ear</packaging>

  <properties>
    <harvester.tmp>${build.directory}</harvester.tmp>
    <logback.staging.directory>${build.directory}/logback</logback.staging.directory>
    <logback.jndi.resource>${jndi.url.harvester.rr2datawell.logback}</logback.jndi.resource>
  </properties>

  <build>
    <resources>
      <resource>
        <directory>${logback.staging.directory}</directory>
        <filtering>true</filtering>
      </resource>
    </resources>

    <plugins>
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-ear-plugin</artifactId>
        <version>${maven.ear.plugin.version}</version>
        <configuration>
          <version>6</version>
          <defaultLibBundleDir>lib</defaultLibBundleDir>
          <filtering>true</filtering>
        </configuration>
      </plugin>

      <plugin>
        <groupId>org.codehaus.mojo</groupId>
        <artifactId>truezip-maven-plugin</artifactId>
        <version>${truezip-maven-plugin.version}</version>
        <executions>
          <execution>
            <id>extract</id>
            <goals>
              <goal>copy</goal>
            </goals>
            <phase>generate-resources</phase>
            <configuration>
              <fileset>
                <directory>${basedir}/../../../commons/utils/logback/target/dataio-commons-utils-logback-${project.version}.jar</directory>
                <outputDirectory>${logback.staging.directory}</outputDirectory>
              </fileset>
            </configuration>
          </execution>
          <execution>
            <id>replace</id>
            <goals>
              <goal>copy</goal>
            </goals>
            <phase>package</phase>
            <configuration>
              <files>
                 <file>
                  <source>${build.directory}/classes/logback.xml</source>
                  <outputDirectory>${build.directory}/dataio-harvester-rr2datawell-ear-${project.version}.ear/lib/dataio-commons-utils-logback-${project.version}.jar</outputDirectory>
                </file>
              </files>
            </configuration>
          </execution>
        </executions>
      </plugin>
    </plugins>
  </build>

  <dependencies>
    <dependency>
      <groupId>dk.dbc</groupId>
      <artifactId>dataio-commons-utils-binary-file-store</artifactId>
      <version>${project.version}</version>
      <type>ejb</type>
    </dependency>
    <dependency>
      <groupId>dk.dbc</groupId>
      <artifactId>dataio-commons-utils-file-store-service-connector</artifactId>
      <version>${project.version}</version>
      <type>ejb</type>
    </dependency>
    <dependency>
      <groupId>dk.dbc</groupId>
      <artifactId>dataio-commons-utils-job-store-service-connector</artifactId>
      <version>${project.version}</version>
      <type>ejb</type>
    </dependency>
    <dependency>
      <groupId>dk.dbc</groupId>
      <artifactId>dataio-commons-utils-logback</artifactId>
      <version>${project.version}</version>
    </dependency>
    <dependency>
      <groupId>dk.dbc</groupId>
      <artifactId>dataio-harvester-utils-rawrepo-connector</artifactId>
      <version>${project.version}</version>
      <type>ejb</type>
    </dependency>
    <dependency>
      <groupId>dk.dbc</groupId>
      <artifactId>dataio-harvester-rr2datawell-ejb</artifactId>
      <version>${project.version}</version>
      <type>ejb</type>
    </dependency>
  </dependencies>

</project>


/*
This is for documentation purposes only, should be auto created by JPA

CREATE TABLE flows (
    id SERIAL NOT NULL PRIMARY KEY,
    data TEXT NOT NULL,
    flowname_idx TEXT NOT NULL UNIQUE
);
*/

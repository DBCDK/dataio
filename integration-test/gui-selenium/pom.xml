<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <parent>
    <artifactId>dataio-integration-test</artifactId>
    <groupId>dk.dbc</groupId>
    <version>1.0-SNAPSHOT</version>
  </parent>

  <artifactId>dataio-integration-test-gui-selenium</artifactId>
  <name>dataio-integration-test-gui-selenium</name>

  <properties>
    <svn.local.dir>${project.build.directory}/svnrepo</svn.local.dir>
  </properties>

  <dependencies>
    <!-- This is ugly, but we need those element ID constants from the GUI views -->
    <dependency>
      <artifactId>dataio-gui</artifactId>
      <groupId>dk.dbc</groupId>
      <version>${project.version}-classbundle</version>
      <scope>system</scope>
      <systemPath>${basedir}/../../gui/target/dataio-gui-${project.version}-classbundle.jar</systemPath>
    </dependency>
    <dependency>
      <groupId>dk.dbc</groupId>
      <artifactId>dataio-integration-test-utils</artifactId>
      <version>${project.version}</version>
      <scope>test</scope>
    </dependency>
    <dependency>
      <groupId>com.h2database</groupId>
      <artifactId>h2</artifactId>
      <version>1.3.171</version>
      <scope>test</scope>
    </dependency>
    <dependency>
      <groupId>org.seleniumhq.selenium</groupId>
      <artifactId>selenium-java</artifactId>
      <version>2.31.0</version>
      <scope>test</scope>
    </dependency>
    <dependency>
      <groupId>org.tmatesoft.svnkit</groupId>
      <artifactId>svnkit</artifactId>
      <version>${svnkit.version}</version>
      <scope>test</scope>
    </dependency>
    <dependency>
        <groupId>dk.dbc</groupId>
        <artifactId>dataio-gui</artifactId>
        <version>${project.version}</version>
        <type>war</type>
    </dependency>
    <dependency>
        <groupId>dk.dbc</groupId>
        <artifactId>dataio-flow-store-service</artifactId>
        <version>${project.version}</version>
        <type>war</type>
    </dependency>
    <dependency>
        <groupId>dk.dbc</groupId>
        <artifactId>dataio-job-store-service</artifactId>
        <version>${project.version}</version>
        <type>war</type>
    </dependency>
    <dependency>
      <groupId>dk.dbc</groupId>
      <artifactId>dataio-sink-service</artifactId>
      <version>${project.version}</version>
      <type>war</type>
    </dependency>
  </dependencies>

  <build>

    <plugins>

      <plugin>
        <artifactId>maven-resources-plugin</artifactId>
        <version>2.6</version>
        <executions>
          <execution>
            <id>copy-integrationtest-resources</id>
            <phase>generate-test-resources</phase>
            <goals>
              <goal>copy-resources</goal>
            </goals>
            <configuration>
              <outputDirectory>${project.build.directory}/integrationtest-resources</outputDirectory>
              <resources>
                <resource>
                  <directory>${basedir}/src/test/resources</directory>
                  <filtering>true</filtering>
                </resource>
              </resources>
            </configuration>
          </execution>
        </executions>
      </plugin>

      <plugin>
        <groupId>com.edugility</groupId>
        <artifactId>h2-maven-plugin</artifactId>
        <version>1.0</version>
        <configuration>
          <port>${h2.port}</port>
        </configuration>
        <executions>
          <execution>
            <id>h2-start</id>
            <phase>generate-test-resources</phase>
            <goals>
              <goal>spawn</goal>
            </goals>
          </execution>
          <execution>
            <id>h2-stop</id>
            <goals>
              <goal>stop</goal>
            </goals>
          </execution>
        </executions>
      </plugin>

    </plugins>
  </build>

  <profiles>

    <profile>
      <id>integrationtest</id>
      <activation>
        <activeByDefault>true</activeByDefault>
      </activation>
      <build>
        <plugins>

          <plugin>
            <groupId>org.glassfish.embedded</groupId>
            <artifactId>maven-embedded-glassfish-plugin</artifactId>
            <version>${glassfish.version}</version>
            <dependencies>
              <dependency>
                <groupId>com.h2database</groupId>
                <artifactId>h2</artifactId>
                <version>1.3.171</version>
              </dependency>
              <dependency>
                <groupId>org.slf4j</groupId>
                <artifactId>slf4j-simple</artifactId>
                <version>1.7.2</version>
              </dependency>
            </dependencies>
            <configuration>
              <goalPrefix>embedded-glassfish</goalPrefix>
              <app>${project.build.directory}/${project.build.finalName}</app>
              <autoDelete>true</autoDelete>
              <contextRoot>${project.name}</contextRoot>
              <systemProperties>
                <property>flowStoreURL=http://localhost:${glassfish.port}/flow-store</property>
                <property>dataioGuiSubversionScmEndpoint=file://${svn.local.dir}</property>
                <!--
                <property>dataioGuiSubversionScmEndpoint=https://svn.dbc.dk/repos</property>
                -->
                <property>dataioJobStoreServiceEndpoint=http://localhost:${glassfish.port}/job-store</property>
                <property>dataioSinkServiceEndpoint=http://localhost:${glassfish.port}/sink-service</property>
              </systemProperties>
              <configFile>${project.build.directory}/integrationtest-resources/domain-${glassfish.version}.xml</configFile>
            </configuration>

            <executions>
              <execution>
                <id>start-glassfish</id>
                <phase>pre-integration-test</phase>
                <goals>
                  <goal>start</goal>
                </goals>
              </execution>
              <execution>
                <id>flowstore-deploy</id>
                <phase>pre-integration-test</phase>
                <goals>
                  <goal>deploy</goal>
                </goals>
                <configuration>
                  <app>../../flow-store-service/target/dataio-flow-store-service-${project.version}.war</app>
                  <contextRoot>flow-store</contextRoot>
                  <name>flowstore</name>
                </configuration>
              </execution>
              <execution>
                <id>jobstore-deploy</id>
                <phase>pre-integration-test</phase>
                <goals>
                  <goal>deploy</goal>
                </goals>
                <configuration>
                  <app>../../job-store-service/target/dataio-job-store-service-${project.version}.war</app>
                  <contextRoot>job-store</contextRoot>
                  <name>jobstore</name>
                </configuration>
              </execution>
              <execution>
                <id>sink-service-deploy</id>
                <phase>pre-integration-test</phase>
                <goals>
                  <goal>deploy</goal>
                </goals>
                <configuration>
                  <app>../../sink-service/target/dataio-sink-service-${project.version}.war</app>
                  <contextRoot>sink-service</contextRoot>
                  <name>sink-service</name>
                </configuration>
              </execution>
              <execution>
                <id>gui-deploy</id>
                <phase>pre-integration-test</phase>
                <goals>
                  <goal>deploy</goal>
                </goals>
                <configuration>
                  <app>../../gui/target/dataio-gui-${project.version}.war</app>
                  <contextRoot>gui</contextRoot>
                  <name>gui</name>
                </configuration>
              </execution>
              <execution>
                <id>stop-glassfish</id>
                <phase>post-integration-test</phase>
                <goals>
                  <goal>stop</goal>
                </goals>
              </execution>
            </executions>
          </plugin>
          <plugin>
            <!-- Integrationtest plugin - just like the surefire plugin,
            but this plugin handles other test files (typically
            testfiles containing IT) -->
            <groupId>org.apache.maven.plugins</groupId>
            <artifactId>maven-failsafe-plugin</artifactId>
            <version>2.14.1</version>
            <executions>
              <execution>
                <goals>
                  <goal>integration-test</goal>
                  <goal>verify</goal>
                </goals>
                <id>integration-test</id>
              </execution>
            </executions>
            <configuration>
              <redirectTestOutputToFile>true</redirectTestOutputToFile>
              <systemPropertyVariables>
                <glassfish.port>${glassfish.port}</glassfish.port>
                <h2.port>${h2.port}</h2.port>
                <svn.local.dir>${svn.local.dir}</svn.local.dir>
              </systemPropertyVariables>
            </configuration>
          </plugin>
        </plugins>
      </build>
    </profile>

    <profile>
      <id>dev</id>
      <properties>
        <glassfish.port>8080</glassfish.port>
        <h2.port>8181</h2.port>
      </properties>
      <build>
        <plugins>
          <plugin>
            <groupId>org.glassfish.embedded</groupId>
            <artifactId>maven-embedded-glassfish-plugin</artifactId>
            <version>${glassfish.version}</version>
            <dependencies>
              <dependency>
                <groupId>com.h2database</groupId>
                <artifactId>h2</artifactId>
                <version>1.3.171</version>
              </dependency>
              <dependency>
                <groupId>org.slf4j</groupId>
                <artifactId>slf4j-simple</artifactId>
                <version>1.7.2</version>
              </dependency>
              <dependency>
                <groupId>dk.dbc</groupId>
                <artifactId>dbc-commons-db</artifactId>
                <version>1.3-SNAPSHOT</version>
              </dependency>
            </dependencies>
            <configuration>
              <goalPrefix>embedded-glassfish</goalPrefix>
              <app>${project.build.directory}/${project.build.finalName}</app>
              <autoDelete>true</autoDelete>
              <contextRoot>${project.name}</contextRoot>
              <systemProperties>
                <property>flowStoreURL=http://localhost:${glassfish.port}/flow-store</property>
              </systemProperties>
              <configFile>${project.build.directory}/integrationtest-resources/domain-${glassfish.version}.xml</configFile>
              <!--
              <configFile>${project.build.directory}/integrationtest-resources/domain-${glassfish.version}_with_pg_template.xml</configFile>
              -->
            </configuration>

            <executions>
              <execution>
                <id>flowstore-deploy</id>
                <phase>this-phase-should-never-run</phase>
                <goals>
                  <goal>deploy</goal>
                </goals>
                <configuration>
                  <app>../../flow-store-service/target/dataio-flow-store-service-${project.version}.war</app>
                  <contextRoot>flow-store</contextRoot>
                  <name>flowstore</name>
                </configuration>
              </execution>
              <execution>
                <id>jobstore-deploy</id>
                <phase>this-phase-should-never-run</phase>
                <goals>
                  <goal>deploy</goal>
                </goals>
                <configuration>
                  <app>../../job-store-service/target/dataio-job-store-service-${project.version}.war</app>
                  <contextRoot>job-store</contextRoot>
                  <name>jobstore</name>
                </configuration>
              </execution>
              <execution>
                <id>sink-service-deploy</id>
                <phase>this-phase-should-never-run</phase>
                <goals>
                  <goal>deploy</goal>
                </goals>
                <configuration>
                  <app>../../sink-service/target/dataio-sink-service-${project.version}.war</app>
                  <contextRoot>sink-service</contextRoot>
                  <name>sink-service</name>
                </configuration>
              </execution>
              <execution>
                <id>sink-deploy</id>
                <phase>this-phase-should-never-run</phase>
                <goals>
                  <goal>deploy</goal>
                </goals>
                <configuration>
                  <app>../../sink/es/ear/target/dataio-sink-es-ear-${project.version}.ear</app>
                  <contextRoot>sink</contextRoot>
                  <name>sink</name>
                </configuration>
              </execution>
              <execution>
                <id>run-glassfish</id>
                <phase>integration-test</phase>
                <goals>
                  <goal>run</goal>
                </goals>
              </execution>
            </executions>
          </plugin>
        </plugins>
      </build>

      <dependencies>
        <dependency>
          <groupId>dk.dbc</groupId>
          <artifactId>dataio-flow-store-service</artifactId>
          <version>${project.version}</version>
          <type>war</type>
        </dependency>
        <dependency>
          <groupId>dk.dbc</groupId>
          <artifactId>dataio-job-store-service</artifactId>
          <version>${project.version}</version>
          <type>war</type>
        </dependency>
        <dependency>
          <groupId>dk.dbc</groupId>
          <artifactId>dataio-sink-service</artifactId>
          <version>${project.version}</version>
          <type>war</type>
        </dependency>
        <dependency>
          <groupId>dk.dbc</groupId>
          <artifactId>dataio-sink-es-ear</artifactId>
          <version>${project.version}</version>
          <type>ear</type>
        </dependency>
              <dependency>
                <groupId>dk.dbc</groupId>
                <artifactId>dbc-commons-db</artifactId>
                <version>1.3-SNAPSHOT</version>
              </dependency>
      </dependencies>

    </profile>
  </profiles>
</project>

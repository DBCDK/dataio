package dk.dbc.dataio.sink.fbs;

import org.junit.Test;

import static org.hamcrest.CoreMatchers.is;
import static org.junit.Assert.assertThat;

public class SinkIT {
    @Test
    public void test() {
        assertThat(true, is(true));
    }
}

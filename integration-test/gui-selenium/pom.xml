<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <parent>
    <artifactId>dataio-integration-test</artifactId>
    <groupId>dk.dbc</groupId>
    <version>1.0-SNAPSHOT</version>
  </parent>

  <artifactId>dataio-integration-test-gui-selenium</artifactId>
  <name>dataio-integration-test-gui-selenium</name>

  <dependencies>
    <!-- This is ugly, but we need those element ID constants from the GUI views -->
    <dependency>
      <artifactId>dataio-gui</artifactId>
      <groupId>dk.dbc</groupId>
      <version>${version}-classbundle</version>
      <scope>system</scope>
      <systemPath>${basedir}/../../gui/target/dataio-gui-${version}-classbundle.jar</systemPath>
    </dependency>

    <dependency>
      <groupId>com.h2database</groupId>
      <artifactId>h2</artifactId>
      <version>1.3.171</version>
      <scope>test</scope>
    </dependency>
    <dependency>
      <groupId>org.seleniumhq.selenium</groupId>
      <artifactId>selenium-java</artifactId>
      <version>2.31.0</version>
      <scope>test</scope>
    </dependency>
  </dependencies>

  <build>
    <plugins>

      <plugin>
        <artifactId>maven-resources-plugin</artifactId>
        <version>2.6</version>
        <executions>
          <execution>
            <id>copy-integrationtest-resources</id>
            <!--
            <phase>pre-integration-test</phase>
            -->
            <phase>generate-test-resources</phase>
            <goals>
              <goal>copy-resources</goal>
            </goals>
            <configuration>
              <outputDirectory>${project.build.directory}/integrationtest-resources</outputDirectory>
              <resources>
                <resource>
                  <directory>${basedir}/src/test/resources</directory>
                  <filtering>true</filtering>
                </resource>
              </resources>
            </configuration>
          </execution>
        </executions>
      </plugin>

      <plugin>
        <groupId>com.edugility</groupId>
        <artifactId>h2-maven-plugin</artifactId>
        <version>1.0</version>
        <configuration>
          <port>${h2.port}</port>
        </configuration>
        <executions>
          <execution>
            <id>h2-start</id>
            <phase>generate-test-resources</phase>
            <goals>
              <goal>spawn</goal>
            </goals>
          </execution>
          <execution>
            <id>h2-stop</id>
            <goals>
              <goal>stop</goal>
            </goals>
          </execution>
        </executions>
      </plugin>

    </plugins>
  </build>

  <profiles>

    <profile>
      <id>integrationtest</id>
      <activation>
        <activeByDefault>true</activeByDefault>
      </activation>
      <build>
        <plugins>
          <plugin>
            <!-- Helper plugin for reserving an unused random port for the glassfish
            server for the selenium tests -->
            <groupId>org.codehaus.mojo</groupId>
            <artifactId>build-helper-maven-plugin</artifactId>
            <version>1.7</version>
            <executions>
              <execution>
                <id>reserve-network-port</id>
                <goals>
                  <goal>reserve-network-port</goal>
                </goals>
                <!--
                <phase>pre-integration-test</phase>
                -->
                <phase>initialize</phase>
                <configuration>
                  <portNames>
                    <portName>glassfish.port</portName>
                    <portName>h2.port</portName>
                  </portNames>
                </configuration>
              </execution>
            </executions>
          </plugin>
          <plugin>
            <groupId>org.glassfish.embedded</groupId>
            <artifactId>maven-embedded-glassfish-plugin</artifactId>
            <version>${glassfish.version}</version>
            <dependencies>
              <dependency>
                <groupId>com.h2database</groupId>
                <artifactId>h2</artifactId>
                <version>1.3.171</version>
              </dependency>
            </dependencies>
            <configuration>
              <goalPrefix>embedded-glassfish</goalPrefix>
              <app>${project.build.directory}/${project.build.finalName}</app>
              <autoDelete>true</autoDelete>
              <contextRoot>${project.name}</contextRoot>
              <systemProperties>
                <property>flowStoreURL=http://localhost:${glassfish.port}/flow-store</property>
              </systemProperties>
              <configFile>${project.build.directory}/integrationtest-resources/domain-${glassfish.version}.xml</configFile>
            </configuration>

            <executions>
              <execution>
                <id>start-glassfish</id>
                <phase>pre-integration-test</phase>
                <goals>
                  <goal>start</goal>
                </goals>
              </execution>
              <execution>
                <id>flowstore-deploy</id>
                <phase>pre-integration-test</phase>
                <goals>
                  <goal>deploy</goal>
                </goals>
                <configuration>
                  <app>../../flow-store-service/target/dataio-flow-store-service-${version}.war</app>
                  <contextRoot>flow-store</contextRoot>
                  <name>flowstore</name>
                </configuration>
              </execution>
              <execution>
                <id>gui-deploy</id>
                <phase>pre-integration-test</phase>
                <goals>
                  <goal>deploy</goal>
                </goals>
                <configuration>
                  <app>../../gui/target/dataio-gui-${version}.war</app>
                  <contextRoot>gui</contextRoot>
                  <name>gui</name>
                </configuration>
              </execution>
              <execution>
                <id>stop-glassfish</id>
                <phase>post-integration-test</phase>
                <goals>
                  <goal>stop</goal>
                </goals>
              </execution>
            </executions>
          </plugin>
          <plugin>
            <!-- Integrationtest plugin - just like the surefire plugin,
            but this plugin handles other test files (typically
            testfiles containing IT) -->
            <groupId>org.apache.maven.plugins</groupId>
            <artifactId>maven-failsafe-plugin</artifactId>
            <version>2.13</version>
            <executions>
              <execution>
                <goals>
                  <goal>integration-test</goal>
                  <goal>verify</goal>
                </goals>
                <id>integration-test</id>
              </execution>
            </executions>
            <configuration>
              <systemPropertyVariables>
                <glassfish.port>${glassfish.port}</glassfish.port>
                <h2.port>${h2.port}</h2.port>
              </systemPropertyVariables>
            </configuration>
          </plugin>
        </plugins>
      </build>
    </profile>

    <profile>
      <id>dev</id>
      <properties>
        <glassfish.port>8080</glassfish.port>
        <h2.port>8181</h2.port>
      </properties>
      <build>
        <plugins>
          <plugin>
            <groupId>org.glassfish.embedded</groupId>
            <artifactId>maven-embedded-glassfish-plugin</artifactId>
            <version>${glassfish.version}</version>
            <dependencies>
              <dependency>
                <groupId>com.h2database</groupId>
                <artifactId>h2</artifactId>
                <version>1.3.171</version>
              </dependency>
              <!--
              <dependency>
                <groupId>org.postgresql</groupId>
                <artifactId>postgresql</artifactId>
                <version>9.2-1002-jdbc4</version>
              </dependency>
              -->
            </dependencies>
            <configuration>
              <goalPrefix>embedded-glassfish</goalPrefix>
              <app>${project.build.directory}/${project.build.finalName}</app>
              <autoDelete>true</autoDelete>
              <contextRoot>${project.name}</contextRoot>
              <systemProperties>
                <property>flowStoreURL=http://localhost:${glassfish.port}/flow-store</property>
              </systemProperties>
              <configFile>${project.build.directory}/integrationtest-resources/domain-${glassfish.version}.xml</configFile>
            </configuration>

            <executions>
              <execution>
                <id>start-glassfish</id>
                <phase>pre-integration-test</phase>
                <goals>
                  <goal>start</goal>
                </goals>
              </execution>
              <execution>
                <id>flowstore-deploy</id>
                <phase>pre-integration-test</phase>
                <goals>
                  <goal>deploy</goal>
                </goals>
                <configuration>
                  <app>../../flow-store-service/target/dataio-flow-store-service-${version}.war</app>
                  <contextRoot>flow-store</contextRoot>
                  <name>flowstore</name>
                </configuration>
              </execution>
              <execution>
                <id>stop-glassfish</id>
                <phase>post-integration-test</phase>
                <goals>
                  <goal>stop</goal>
                </goals>
              </execution>
            </executions>
          </plugin>
        </plugins>
      </build>

    </profile>
  </profiles>
</project>

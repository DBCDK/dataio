#!/bin/bash

BASEDIR=$(cd "$(dirname "$0")"; pwd)/..

STARTUP_SCRIPT=$BASEDIR/home/amqRemoteInstall.sh
SHUTDOWN_SCRIPT=$BASEDIR/bin/stop_activemq

sh -c "$SHUTDOWN_SCRIPT"

mvn -f $BASEDIR/pom.xml generate-resources
chmod a+x $BASEDIR/home/amq*

sh -c "$BASEDIR/home/amqRemoteInstall.sh"
